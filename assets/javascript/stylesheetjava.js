// style sheet working JS
$( document ).ready(function() {
  // When the page loads
    $('.container').hide()
    $(".carousel-item").on("click", function (){
          $('.container').show();
        })
      })